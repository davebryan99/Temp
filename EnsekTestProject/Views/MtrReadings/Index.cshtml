@model IEnumerable<EnsekTestProject.Models.MtrReading>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AccountId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MeterReadingDateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MeterReadValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AccountId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MeterReadingDateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MeterReadValue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MtrReadingId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MtrReadingId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MtrReadingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
